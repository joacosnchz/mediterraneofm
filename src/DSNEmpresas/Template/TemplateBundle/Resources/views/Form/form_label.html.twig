{% extends 'form_div_layout.html.twig' %}

{% block form_label -%}
	<style>
		#required {
			color: red,
		}
	</style>
	
	{{ parent() }}
	{% if label %}
		{% if required %}
			<span id="required">*</span>
		{% endif %}
	{% endif %}
{%- endblock form_label %}